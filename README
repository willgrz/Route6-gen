#Route6-gen 
#Generates RIPE route objects with pingable IP (assumed ::1 of the network) and correct location (taken from RIPE WHOIS)
#Usage: put networks with netmask in networks.txt (see example in it), make sure your RIPE entry is in the correct format (whois one of the networks in the networks.txt for example)
